jms_serializer:
    property_naming:
        separator:
        lower_case: false
        enable_cache: true

    metadata:
        cache: file
        debug: "%kernel.debug%"
        file_cache:
            dir: "%kernel.cache_dir%/serializer"
